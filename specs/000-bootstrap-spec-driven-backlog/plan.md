# Plan — SPEC-000

1. Create `specs/` as the product backlog root.
2. Define lifecycle/status conventions.
3. Backfill a SPEC for each implemented capability visible in current product architecture/documentation.
4. Register planned capabilities already accepted as roadmap direction.
5. Require future work to evolve through the canonical Spec Kit flow rather than ad-hoc PR numbering.