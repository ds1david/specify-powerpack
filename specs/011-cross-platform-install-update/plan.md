# Plan — SPEC-011

Maintain Python installer with thin shell/PowerShell wrappers; bootstrap uv/Spec Kit when required; install canonical CLI plus compatibility alias; implement safe update/check/project-refresh/recovery paths.